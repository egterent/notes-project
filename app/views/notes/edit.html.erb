<% provide(:title, 'Edit note') %>
<% provide(:button_text, 'Save changes') %>
<h1>Update the note</h1>

<div class="row d-flex justify-content-center">
  <div class="col-md-6 col-md-offset-3">
    <%= link_to 'Back', @note.category %>
    <%= render 'form' %> 
  </div>
</div>
